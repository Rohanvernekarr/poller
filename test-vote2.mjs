import { prisma } from './packages/db/index.ts'; async function run() { const poll = await prisma.poll.create({data: {title: 'Test Poll', options: {create: [{text:'Option 1'}, {text:'Option 2'}]}}, include: {options: true}}); console.log('Poll:', poll.id); const res = await fetch('http://localhost:3000/api/vote', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({pollId: poll.id, optionId: poll.options[0].id, fingerprintClient: 'myfp'}) }); console.log(res.status, await res.text()); } run().catch(console.error);
