import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function r() { const p = await prisma.poll.findFirst({include: {options: true}}); console.log(p.id, p.options[0].id); try { await prisma.vote.create({ data: { pollId: p.id, optionId: p.options[0].id, fingerprintHash: '123_hash', ipAddress: '127.0.0.1', userAgent: 'test' }}); console.log('success'); } catch(e) { console.error('FAILED:', e.message); } } r().catch(console.error);
